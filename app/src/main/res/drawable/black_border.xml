<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- רקע לבן -->
    <stroke
        android:color="@android:color/black"
        android:width="2dp" /> <!-- קו תחתון בעובי 2dp -->
    <corners android:radius="0dp" /> <!-- לא מעגל את הפינות -->
    <padding
        android:left="0dp"
        android:right="0dp"
        android:top="0dp"
        android:bottom="0dp" /> <!-- לא משנה את ה-padding -->
</shape>

